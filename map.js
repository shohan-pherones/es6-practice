const numbers = [54, 12, 66, 87, 32, 41, 54, 21, 20, 87, 50];
let sum = 0;

// for (const number of numbers) {
//   sum = sum + number;
// }
// console.log(sum);

// const output = numbers.map((number) => (sum = sum + number));
// console.log(sum);

const square = numbers.map((num) => num * 2);
console.log(square);
